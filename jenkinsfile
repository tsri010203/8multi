pipeline{
    agent any
    stages{
        stage('checkout_master'){
            steps{
               git branch: 'dev', url: 'https://github.com/tsri010203/taxi-booking.git'

            }
        }
        stage('Build_master'){
            steps{
                sh "mvn package"
            }
        }
}
